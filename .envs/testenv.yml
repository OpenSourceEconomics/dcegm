---
name: dcegm
channels:
  - conda-forge
  - nodefaults
dependencies:
  - pip
  - setuptools_scm
  - toml
  - jax
  - numpy
  - pandas
  - yaml

  # Testing infrastructure
  - pytest
  - pytest-cov
  - pytest-xdist

  # Install dcegm locally
  - pip:
      - -e ../
