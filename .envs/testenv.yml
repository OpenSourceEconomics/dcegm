---
name: dcegm
channels:
  - conda-forge,
  - nodefaults
dependencies:
  - setuptools_scm
  - toml

  # Package dependencies
  - jax
  - numpy
  - pandas

  # Testing infrastructure
  - pytest
  - pytest-cov
  - pytest-xdist
