---
name: dcegm
channels:
  - conda-forge,
  - nodefaults
dependencies:
  - setuptools_scm
  - toml

  # Package dependencies
  - dags
  - jax>=0.3.0
  - jaxlib>=0.3.0
  - numpy
  - pandas

  # Testing infrastructure
  - pytest
  - pytest-cov
  - pytest-xdist
