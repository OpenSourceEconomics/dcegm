---
name: dcegm
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - pytest
  - pytest-cov
  - pytest-xdist

  # Package dependencies
  - numpy
  - scipy
  - numba
  - pandas
  - jax

  # Misc
  - black
  - flake8
  - jupyterlab
  - matplotlib
  - pdbpp
  - pre-commit
  - setuptools_scm
  - toml

  # Conda
  - anaconda-client
  - conda-build
  - conda-verify
  - tox-conda
  - pip:
      - blackcellmagic
      - furo
      - upper-envelope
      - -e .  # Install dcegm locally
